/**
 * Re-export from @okazaki/shared-renderer/types — single source of truth.
 * Mantido apenas para imports antigos (`../content/types`) continuarem
 * funcionando sem refactor em massa.
 */

export type {
  Block,
  Section,
  PageFaq,
  MetaChip,
  HowToStep,
  PageContent,
} from "@okazaki/shared-renderer/types";
